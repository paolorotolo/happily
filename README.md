# Happily: an Android Experiment

Happily is a simple Android experiment that uses Google's Mobile Vision API to detect if the user is smiling.

If yes, it animates the background and loads an happy quote.

